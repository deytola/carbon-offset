type Tree {
  id: ID!
  species: String!
  age: Int!
}


type Query {
  tree(id: ID!): Tree
  trees: [Tree!]!
}

input CreateTreeInput {
  species: String!
  age: Int!
}

type Mutation {
  createTree(treeInput: CreateTreeInput!): Tree!
}

