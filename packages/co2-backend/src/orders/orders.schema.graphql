type Order {
    id: ID!
    fkUserId: ID!
    fkTreeId: ID!
    quantity: Int!
    totalPrice: Float!
}


type Query {
    order(id: ID!): Order
    orders: [Order!]!
}

input CreateOrderInput {
    fkUserId: Int!
    fkTreeId: Int!
    quantity: Int!
    totalPrice: Float!
}

type Mutation {
    createOrder(orderInput: CreateOrderInput!): Order!
}

